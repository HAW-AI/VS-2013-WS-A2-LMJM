#!/usr/bin/env escript

-compile(node_start).
-compile(util).
-compile(node).

main(_) ->
  Path = "nodes/",

  lists:foreach(
    fun(Num) -> node_start:main(lists:concat([Path, "node_", Num, ".conf"])) end,
    lists:seq(0, 6)
  ),
  io:fread("", "~c").
